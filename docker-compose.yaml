version: "3.9"

services:
  server:
    build: ./server
    ports:
      - "8000:8000"
    restart: always

  web:
    build: ./website_dummy
    ports:
      - "8080:8080"
    depends_on:
      - server
    restart: always